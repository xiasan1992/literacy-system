# 汉字识别学习系统 - 需求规格说明文档 (REQUIREMENTS.md)

## 1. 项目背景与目标

为满足幼小衔接及儿童识字需求，开发一套运行于电脑端的汉字识别与测试系统。系统旨在通过大字号视觉呈现、趣味互动以及科学的错题复习机制，帮助儿童高效、牢固地掌握常用汉字（如《小羊上山》分级读物生字库）。

---

## 2. 角色与使用场景

- **主要使用者（儿童）**：通过电脑大屏观看汉字并进行朗读识别。
- **辅助使用者（家长/教师）**：输入或选择指定汉字组，根据儿童回答点击“认识”或“不认识”进行测试，查看统计结果。

---

## 3. 功能需求规格

### 3.1 汉字展示与界面交互 (UI/UX)
1. **屏幕占比要求**：测试与复习页面中，汉字展示区及字号须占据屏幕 80% 左右的视口大小 (`80vmin`)。
2. **田字格辅助**：字框内置淡蓝色装饰性田字格背景，字体采用标准楷体（"KaiTi" / "Kaiti SC"），符合规范汉字书写特征。
3. **动画与反馈**：
   - 切换汉字时具备弹性缩入动画。
   - 点击“认识”（绿色框发光）与“不认识”（红色框发光）时提供微动画反馈。

### 3.2 汉字输入与预设字库
1. **自定义汉字输入**：支持在首页文本域中手动输入、粘贴汉字，自动过滤非中文字符并去重。
2. **预设汉字组选择**：
   - 提供《小羊上山》第 1 到第 6 级完整生字库预设。
   - 提供常用基础、自然、身体、家庭、动物、颜色等分类预设。
3. **选择覆盖机制**：点击预设卡片时直接**覆盖**当前输入框文本，防止重复追加。

### 3.3 汉字掌握算法 (Scoring & Mastery Logic)
1. **基础计分**：针对每个汉字记录累计答对次数 (`correct`) 和累计答错次数 (`wrong`)。
2. **连胜机制 (`streak`)**：记录每个汉字最近连续答对的次数。
3. **已掌握判定标准 (`isMastered`)**：
   - **规则 A（首次即对）**：历史答错次数 `wrong == 0` 且答对次数 `correct > 0`。
   - **规则 B（错题消除）**：若曾有答错记录（`wrong > 0`），之后必须**连续答对 3 次**（`streak >= 3`）。

### 3.4 常用汉字组掌握率与错字提示
1. **实时掌握率分析**：在预设卡片下方动态计算并展示掌握率百分比及掌握数量（例如：`掌握率: 80% (48/60)`）。
2. **未掌握字实时罗列**：在卡片下方列出当前组内所有未掌握的汉字；若全部掌握，则显示 `🎉 全部掌握！`。

### 3.5 错题集与复习模式
1. **全局错题复习**：从所有已测试过的汉字中提取未掌握的字进行集中测试。
2. **单组错题复习（“复习其中错字”）**：在首页选择某预设组或输入汉字后，支持仅抽取该范围内未掌握的错字进行专项复习。

### 3.6 数据持久化与统计
1. **本地持久化**：使用 `localStorage` 自动实时保存 `charsData`，页面关闭或重启后记录不丢失。
2. **数据清理**：提供二次确认的“清除记录”功能。
3. **统计大盘**：
   - 汇总显示已学汉字数、总答对/答错次数、整体正确率。
   - 提供按“全部”、“已掌握”、“需复习”分类筛选查看汉字卡片。

---

## 4. 非功能性需求

1. **易用性**：界面控件大、色彩明快，适合儿童及家长操作。
2. **性能需求**：纯前端无后端依赖，页面秒开，粒子背景与卡片切换保持 60fps 流畅度。
3. **兼容性**：支持 Windows / macOS 上各主流桌面浏览器（Chrome、Edge、Safari、Firefox）。
